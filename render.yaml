services:
  - type: web
    runtime: docker
    name: huijibu
    plan: free
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        value: sqlite:///./huijibu.db
